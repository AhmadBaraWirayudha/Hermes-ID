# Backups

SQLite and workspace backups generated by the app and CLI.

Commands:

```bash
python app/cli.py backup-db
python app/cli.py backup-workspace
python scripts/verify_backup.py backups/<backup>.bak
```
