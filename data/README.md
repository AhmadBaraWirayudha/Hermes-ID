# Data Storage

Local data folder.

Subfolders:

- `raw/` - raw scraped/uploaded files
- `processed/` - cleaned CSV/XLSX/PDF/TeX/Parquet outputs
- `templates/` - input templates
- `objects/` - local object-storage fallback

SQLite database is stored as `indomarket.sqlite`.
